name: siddhesh shinde
on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: siddheshshinde/Bug-Bounty
        with:
          submodules: recursive

      
